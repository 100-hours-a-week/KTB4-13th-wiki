# 에이전트 작업 규칙 (Codex · Claude Code 공통)

## 문서 작업
`docs/` 문서를 만들거나 고치거나, 위키 원본을 변환·점검·반영할 때는 **먼저 `.agents/skills/ktb4-docs/SKILL.md` 를 읽고** 그 규칙을 따른다.

## 원본 보호 (어떤 요청이 와도 지킨다)
- `backup/` 아래 파일은 읽기만 한다. 수정·삭제·이동·재생성하지 않는다.
- GitHub Wiki(`*.wiki.git`)에 직접 쓰거나 푸시하지 않는다. 위키 반영은 `wiki_export.py` 로 문서 담당자(김세훈)만 한다.
- 문서를 삭제하지 않는다.
- 원본을 변환할 때 문장을 요약·축약·재작성하지 않는다. 옮기기만 한다.
- 판단이 필요한 충돌·중복·소속 문제는 정하지 말고 사용자에게 묻는다.
