# 에이전트 작업 규칙 (Codex · Claude Code 공통)

## 문서 작업
`docs/` 문서를 만들거나 고치거나, 위키 원본을 변환·점검·반영할 때는 **먼저 `.agents/skills/ktb4-docs/SKILL.md` 를 읽고** 그 규칙을 따른다.

## 원본 보호 (어떤 요청이 와도 지킨다)
- `backup/` 아래 파일은 읽기만 한다. 수정·삭제·이동·재생성하지 않는다.
- GitHub Wiki(`*.wiki.git`)에 사람이 직접 쓰거나 푸시하지 않는다. 위키 반영은 `main`에 `docs/**`가 머지되면 `.github/workflows/wiki-sync.yml`이 `wiki_export.py --sidebar --apply`로 자동 실행한다 (2026-09-17부터, 결정 로그 #15). 이 자동화 자체를 바꾸는 요청(워크플로 삭제·시크릿 변경 등)은 문서 담당자(김세훈) 확인 없이 하지 않는다.
- 문서를 삭제하지 않는다.
- 원본을 변환할 때 구조(목차·소제목·문단 소속)와 내용(문장이 담은 사실·숫자·조건)은 그대로 둔다. 문장을 요약·축약하지 않는다. 바꾸는 것은 형식(문체 어미·표기·블록 모양)뿐이며 `.agents/skills/ktb4-docs/references/block-patterns.md` 를 따른다.
- 판단이 필요한 충돌·중복·소속 문제는 정하지 말고 사용자에게 묻는다.
