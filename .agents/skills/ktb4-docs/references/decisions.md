# 팀 결정 (2026-09-17 확정)

| # | 항목 | 결정 |
|---|---|---|
| 1 | 문체 | 새로 쓰는 문장은 "~한다". 변환 문서도 **문장 끝 어미만** "~한다"로 통일한다 (09-17 #16으로 갱신. 어미 외 단어·메시지 문자열은 그대로) |
| 2 | 영어 용어 | 새 문장은 한국어 우선, 페이지 첫 등장만 원문 병기. 도구·서비스 이름은 원문. 변환 원문은 그대로 (변환 문서 용어 통일은 미결) |
| 3 | 파트 코드 | `FE` `BE` `FS`(풀스택 공동) `AI` `CLD` `PM`(기획) `REL` `SPR` `DEC`(결정) `TEAM`(공통) |
| 4 | 하위 번호 | 단계 번호까지만: `CLD-3 CD 파이프라인 설계`. 절 번호는 이름에 넣지 않는다 |
| 5 | 분량 상한 | **새 문서 전용.** 설계 150줄·섹션 5 / 선택 근거 100줄·결정 5 / 명세 250줄·항목 8 / ADR 40줄. 넘치면 자르지 않고 부록으로. 변환 문서는 초과가 경고이며 부록 대신 제자리에서 접는다 (#16) |
| 6 | 문서 담당 | 김세훈 1명 (점검·백업). 위키 반영은 09-17부터 CI 자동화로 대체 (#14) |
| 7 | 스킬 위치 | 메인 레포 `KTB4-13th-wiki`, 원본 `.agents/skills/ktb4-docs/` |
| 8 | AI 도구 | Codex 4명, Claude Code 2명 → 두 도구 모두 지원 |
| 9 | 위키 반영 방식 | 원본 페이지는 그대로 두고 **새 이름 페이지를 추가**, 사이드바를 새 페이지로 연결 |
| 10 | 수정 위치 | 변환이 끝난 문서는 **레포 `docs/` 에서만** 수정. 아직 변환 안 된 문서는 원본 위키에서 수정 |
| 11 | 결정 기록 | `DEC-000 결정 로그` 한 곳에 한 줄씩, 설명이 길 때만 ADR |
| 12 | API 명세 | 위키의 FS API 명세, AI API 명세 두 개가 기준 (Google Sheet 제외). 공통 형식(응답 `{message, data}`, 에러 표기)을 맞춘다 |
| 13 | 설계·근거 | **새 문서 전용.** 설계와 선택 근거는 다른 파일, 같은 group. 변환은 1 원본 페이지 = 1 문서이며 나누지 않는다 (#16) |
| 14 | 위키 반영 자동화 (09-17 추가) | `main`에 `docs/**` 머지되면 `.github/workflows/wiki-sync.yml`이 `wiki_export.py --sidebar --apply`를 자동 실행하고 위키에 직접 push. **사람이 위키에 push하는 단계 없앰.** 대신 PR 단계의 기존 CI(`docs-check.yml`: preserve_check·check_docs)가 이미 머지 전에 오류 0을 강제하므로, "위키에 반영되기 전 마지막 사람 확인"은 위키 push 시점이 아니라 **PR 리뷰 시점**으로 옮겨간 것. GitHub Wiki엔 PR 리뷰가 없어 push 자체엔 리뷰가 없다는 점을 감수하기로 함. `WIKI_DEPLOY_TOKEN` 시크릿(레포 write 권한 PAT) 설정 필요 — 문서 담당자가 GitHub 설정에서 직접 추가. (참고: `docs/convert-ai-1` PR에 있는 "옮김표 사전 승인 생략" 결정과 번호가 겹칠 수 있음 — 먼저 머지되는 쪽 기준으로 나중에 정리) |
| 15 | 변환 승인 방식 (09-17 추가) | 절차가 무겁다는 피드백 → **변환 모드에서 옮김표 사전 승인 생략.** 요청 한 번에 변환·검사·PR까지 끝내고 결과를 한 번에 보고. 원문 보존 기준(요약 금지, preserve_check 0건)과 "충돌 기준 없을 때 사용자에게 묻기"는 그대로 유지 — 줄인 건 승인 횟수지 검사 기준이 아니다 |
| 16 | 변환 = 형식 통일 (09-17 추가) | "원문 그대로 옮기기"만으로는 세 파트 문서의 형식 차이(문체·제목 마크업·펜스·`<details>` 중첩·레이블 이름·목록 깊이)가 그대로 남는다는 피드백 → 변환 원칙을 세 층으로 나눔. **구조(목차·소제목·문단 소속·페이지 단위) 불변, 내용(사실·숫자·조건·부정·한정·코드·식별자) 불변, 형식(문체 어미·표기·블록 모양)은 `style-guide.md`·`block-patterns.md` 로 통일.** 세부: ① 1 원본 페이지 = 1 문서, 설계·근거·부록으로 나누지 않음 ② 문체 어미 `~한다` 통일 (#1 갱신) ③ 굵은 줄로 쓴 절 제목과 항목을 묶는 바깥 `<details>`는 글자 그대로 `###` 제목으로 (목차·앵커에 잡히게) ④ 긴 예시·코드는 부록 대신 제자리 `<details>`. 부록 분리는 파트 결정이 있을 때만 (현재 AI-1) ⑤ 문장 손질(분할·조건 앞으로)은 기본 꺼짐, 파트 요청 시만 ⑥ 원본 충돌은 양쪽 보존 + 기준값 아닌 문장 아래 `> 기준:` 한 줄. 검사: `preserve_check` 에 제목 트리 대조(오류) 추가, 문체 어미 차이는 무시 |
| 17 | 원본 보존 검사 CI 제외 (09-21 추가) | docs가 실제 원본이 되어 설계가 계속 바뀌는데 `preserve_check`가 09-17 백업과 영구 비교하는 탓에 정상적인 수정(#124 Qdrant→pgvector)도 오류가 되었고, `wiki-sync.yml`까지 실패해 위키에 반영되지 않았음 → **`docs-check.yml`과 `wiki-sync.yml`에서 `preserve_check` 제거.** 백업 수정 금지·백업 무결성·`check_docs`는 유지. `preserve_check.py`는 남겨두고, 아직 변환 안 된 페이지(ai-9, ai-10, cld-4)를 변환할 때 변환 모드 6번에서 **로컬로** 실행해 결과를 PR 본문에 적는다. #14의 "PR 단계 CI가 preserve_check 오류 0을 강제" 부분은 이 결정으로 대체 |

## 파트별 결정

| 파트 | 결정 |
|---|---|
| FS | 파트 위키(Frontend Wiki, Backend Wiki)를 허브로 사용. 공통 형식 맞춤. 앞으로 바뀔 문서는 미정 → 일단 진행 |
| AI | 모델 API 설계의 요청·응답 예시는 부록으로 분리 (AI-1에 한함. #16 이후 기본은 제자리 접기). ERD 문서는 아직 없음 → 참조는 `ERD (작성 예정)` 로 표시 |
| CLD | 3단계 상위 페이지와 `3 5` 가 다르면 **상위 페이지 값이 기준** (배포 중 API 중단 최대 5분, Secret은 각 EC2 `.env.production`). `3 5` 문서는 그대로 두고 다른 문장 아래 `> 기준: 3단계 상위 페이지 값 — 결정 로그 09-17` 한 줄 추가 (#16, 부록 "원본 차이" 대신). 4단계는 작성이 끝난 뒤 변환 |
| 기획 | 스프린트 플래닝은 한 페이지를 계속 갱신 |

## 원본 페이지 판정

| 원본 | 판정 |
|---|---|
| `1 1 현재 서비스의 성장 상황과 기존 인프라 한계` | `1 1 현재 서비스의 성장 상황` 과 내용 동일한 중복. 변환 제외, 백업에만 보관 |
| 5단계, 6단계 | 원본 페이지 없음. 사이드바에 "작성 예정" 텍스트로만 |
| `2.3`, `2.4` (4단계 하위) | 원본 페이지 없음 (링크만 존재) |
